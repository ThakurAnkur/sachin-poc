const defaultState = ()=>({cartAmount: 1000})
const state = defaultState();

const getters = {

};

const mutations = {}

const actions = {}
export default {
    namespace: true,
    state,
    getters,
    mutations,
    actions,
}
